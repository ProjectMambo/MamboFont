# Archive

`v0.2/` is the frozen pre-generator repository layout: the monolithic SVG drawing, 252 exported glyph SVGs, site/brand artwork, historical font binaries, documentation, and the old export/release CLI.

Nothing here is used by the current build. It remains available for visual comparison and recovery while the 0.3 candidate is reviewed.
