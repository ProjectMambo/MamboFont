# Archive

`v0.2/` is the frozen pre-generator repository layout: the monolithic SVG drawing, 252 exported glyph SVGs, site/brand artwork, historical font binaries, documentation, and the old export/release CLI.

`v0.3-stroke-prototype/` preserves the rejected FontForge-stroke prototype and the deferred Mambo Icons work from source commit `58906c8`.

Nothing here is used by the current build. It remains available for visual comparison and recovery while the next candidate is reviewed.
