# v0.3 stroke prototype

This snapshot preserves work from source commit `58906c8` that is not part of the active build.

- `sources/icons.py` and `dist/MamboIcons-*` are the deferred Mambo Icons prototype.
- `specimen.html` is the mixed text-and-icon review page generated by that prototype.

The active milestone now covers MamboFont only. Nothing in this directory is imported, built, or released.
