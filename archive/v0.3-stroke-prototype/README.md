# v0.3 stroke prototype

This snapshot preserves rejected work that is not part of the active build. Its geometry and candidates come from source commit `58906c8`; the text-only compiler and test reflect checkpoint `81d93ac`.

- `sources/`, `script/`, and `tests/` contain the rejected centerline-and-stroke implementation.
- `dist/` contains its text and deferred Mambo Icons candidates.
- `specimen.html` and `specimen-text.html` preserve its mixed and text-only review pages.

The active milestone now covers MamboFont only. Nothing in this directory is imported, built, or released.
